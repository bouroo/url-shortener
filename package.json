{
  "name": "url-shortener-monorepo",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev:frontend": "bun --filter frontend dev",
    "build:frontend": "bun --filter frontend build",
    "deploy:frontend": "bun --filter frontend deploy",
    "dev:worker": "bun --filter worker dev",
    "build:worker": "bun --filter worker build",
    "deploy:worker": "bun --filter worker deploy"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}